import Foundation

public enum FeatureManagement {
    public enum Data {
        public enum DBScheme_1_0 {}
    }

    public enum Business {
        public enum Model {

        }
    }

    public enum UI {

    }
}
